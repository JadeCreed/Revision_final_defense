// src/pages/admin/UserManagement.jsx
// Admin → User Management page
// Connected to: /admin/users route in AppRoutes.jsx
// Feature: list, approve, create AT/BRGY users (build here later)

const UserManagement = () => {
  return (
    <div>
      <h1 style={{ fontSize: '1.5rem', fontWeight: '700', marginBottom: '0.5rem' }}>
        User Management
      </h1>
      <p style={{ color: '#6b7280', marginBottom: '1.5rem' }}>
        Manage farmers, AT, and Barangay President accounts here.
      </p>
      {/* Build your table, forms, filters here later */}
    </div>
  );
};

export default UserManagement;